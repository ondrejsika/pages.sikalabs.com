[Install Terraform](/install_terraform)

# Install Terraform on Mac

## Using Homebrew (Recommended)

The easiest way to install Terraform on macOS is using Homebrew:

```bash
brew install hashicorp/tap/terraform
```

## Verify Installation

Check that Terraform is installed correctly:

```bash
terraform version
```
