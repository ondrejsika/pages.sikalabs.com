[Install Vault](/install_vault)

# Install Vault on Mac

## Using Homebrew (Recommended)

The easiest way to install Vault on macOS is using Homebrew:

```bash
brew install hashicorp/tap/vault
```

## Verify Installation

Check that Vault is installed correctly:

```bash
vault version
```
